# 2026-06-11 — Dev day status

> Forward backlog. Planned/unfinished sprints carried from [`../20260610/status.md`](../20260610/status.md) and earlier; everything **done** stays archived under its dev-day `done/`.
> **Reports (this day):** [01 — Shopware vs vbwd user product comparison](reports/01-shopware-vs-vbwd-user-product-comparison.md) · [02 — bot rich rendering + portable styles (S70/S71)](reports/02-bot-rich-rendering-and-portable-styles.md) · [03 — S68 CMS widget menu export + deletion](reports/03-s68-cms-widget-menu-export-and-deletion.md) · [04 — S65 demo-data import: prod walkthrough (hotel + doctor)](reports/04-s65-demo-data-import-prod-walkthrough.md) · [05 — S66 ≡ S67.1 backend APNs push](reports/05-s66-s67-1-backend-apns-push.md) · [06 — S67.2 iOS notifications + badges](reports/06-s67-2-ios-notifications-and-badges.md) · [07 — S69 plan/add-on driven permissions](reports/07-s69-plan-addon-driven-permissions.md) · [08 — core `--full` gate green (CI + host paths)](reports/08-core-full-gate-green.md).
>
> **🟢 Recently done (2026-06-12):** **S72 — Taxes & Countries (all 4 sub-sprints)** — **72.1** Countries+Taxes moved to a dedicated `/admin/settings/tax-and-countries` page (extracted tabs, ~640 dead lines removed from Settings.vue). **72.2** core `taxes`+`tax_classes` data-exchange exchangers (JSON+CSV; FK carried by tax-class `code`, unknown code → row error not 500) + `ImportExportControls` embedded on both tabs. **72.3** each sellable entity (plan/product/resource) gets a M2M join to the **core** `vbwd_tax` catalog (`ON DELETE RESTRICT` → in-use tax delete blocked, never FK 500), a `DualListSelector` "Taxes" block (shared `useTaxOptions` composable), and pricing summing applied rates into net/tax/gross. **72.4** global `prices_display_mode` core setting (brutto default) + nullable per-entity `price_display_mode` override; all 3 public payloads expose `effective_display_mode` (=override ?? global) + the global; fe-user shared `PriceDisplay` renders net/gross with a "netto price" tag when an item differs. Core stays agnostic (plugins reference the core Tax catalog; core imports no plugin). Gates green: core (1430u/36i data-exchange + settings), `--plugin {subscription,shop,booking} --full` (tax + display-mode), fe-admin 776 Vitest, fe-user 384+ Vitest. 5 plugin migrations applied to the dev DB; override mechanism verified live (admin PUT netto → public `effective_display_mode:netto` under global brutto → tag fires; invalid value → 400). **Known gap:** shop/booking **list** payloads carry no `pricing` block, so only their detail views show net/gross+tag (plan list+detail both covered); invoice/checkout multi-tax splitting deferred to S72.5. On disk, not committed. Sprints: [done/s72](done/s72-taxes.md).
>
> **🟢 Recently done (2026-06-12):** **S69 — plan/add-on driven user permissions** — a tariff plan or add-on can declare RBAC permissions in its existing JSON config (`features.permissions_enable` / `special_permissions_enable`, same keys on `AddOn.config`); the user gains/loses them as the source goes ACTIVE/TRIALING → cancelled/expired. Core adds **one narrow write port** `IUserPermissionGrant` (`vbwd/services/user_permission_grant.py`, mirrors the `entitlement.py` registry — idempotent ensure/sync/assign/revoke, unknown-perm skip, **D4 guardrail**: special/admin-track perms gated behind `allow_plan_special_permissions` default-off + `*`/`settings.system` deny-list). Subscription is a **reconcile-not-ad-hoc consumer** (`PermissionSyncService.reconcile_user` — union of active plans+addons, managed `auto-plan-*`/`auto-addon-*` `is_system` levels/roles, overlap-safe, no core-model imports) driven by lifecycle events, with the **D5 emit gaps closed** (admin activate/cancel routes, scheduler expiry, and new `addon.activated`/`addon.cancelled`). No DB migration. DoD gate `--plugin subscription --full` **green** (144 unit / 79 integration); core port 9/9; 19 new tests. Core `--full` has only **pre-existing, non-S69** meta-ratchet failures (seed_marker repo-test gap red since S30; ambient booking datetime drift — report 07 §Core-gate honesty). fe-admin editor deferred **S69.1**. On disk, not committed. See report 07.
>
> **🟢 Recently done (2026-06-12):** **S79 — Withdraw (token balance → money payout)** — new plugin duo + payout capability on all 4 payment providers. Core gains only the `PayoutProvider` ABC (sibling to `PaymentProviderPlugin`, `vbwd/plugins/payment_provider.py`) + `TokenTransactionType.WITHDRAW` (native-PG-enum → core migration). NEW `plugins/withdraw`: `IWithdrawableBalance` registry (token source today; **S80 earnings source plugs into the same seam**), `WithdrawRequest` model + own-branch migration, debit-once-on-request / admin-approve→payout / refund-on-failure-or-reject service, 7-route API. The four providers each implement payout: **paypal** (Payouts API v1), **stripe** (Connect transfers), **truemoney** (HMAC wallet transfer), **promptpay** (bank-client funds-transfer seam) — proven substitutable by a shared parametrised Liskov contract suite. NEW `vbwd-fe-user/plugins/withdraw`: reusable schema-driven **`WithdrawPanel`** (balance + money preview, provider radios, generated destination form, request history) + `/withdraw` page + `/withdraw/:id` detail, `WithdrawPanel` exposed via `sdk.addComponent` for S80 reuse. Gates green: backend `--plugin {withdraw,paypal,stripe,truemoney,promptpay} --full` (withdraw 48u/5i + 318 provider/withdraw unit + shared contract suite), fe-user 840 Vitest + ESLint. Live `GET /withdraw/providers` lists all 4 with destination schemas. On disk, not committed. Sprint: [done/s79](done/s79-withdraw.md).
>
> **🟢 Recently done (2026-06-12):** **S67.2 (iOS) — notifications, side-menu badge, app-icon badge** — the S67 feature is now complete on both ends: host app gains the APNs bridge (`VBWDAppDelegate` + entitlements + `UIBackgroundModes`, foreground suppress-when-conversation-open policy, tap-to-route with cold-start buffer), core gains the agnostic `sdk.notifications` seam (`DefaultNotificationsSDK` actor — token buffering/replay to late sinks, injectable badge setter) + `BadgeProvider`/`MenuBadgePill` on menu items, meinchat wires an auth-gated `MeinChatTokenSink` (JWT-only register, re-register on re-login, best-effort unregister on logout), once-per-install permission prompt, and `InboxViewModel.unreadTotal` driving both the side-menu pill and the app-icon badge; meinchat-plus mirrors with `app="meinchat-plus"`. **Bonus fix: `boundary-lint.sh` was silently dead** (scanned a non-existent path) — now live with narrow UIKit allowlists. Gates green (core runner 573, meinchat 58/0, plus 40, app BUILD SUCCEEDED w/ `aps-environment` in `.xcent`); 23 new tests. **Open: real-device APNs end-to-end proof** (needs device + portal capability + `.p8`). On disk, not committed. See report 06.
>
> **🟢 Recently done (2026-06-12):** **S81 — Import from WordPress (RSS → CMS posts)** — new plugin duo: backend `plugins/wp_import` (FeedReader for tokenized feeds w/ forced read-tracking-off + token masking, paginated-REST term classification **categories-set-else-tag**, REST `featured_media` enrichment for text-only feeds, image re-hosting, GUID dedup w/ reimport-after-complete-removal) + fe-admin `plugins/wp-import` (**first consumer of the `dataExchangeTabs` extension point**: WP Import tab on `/admin/import-export` — feed stats, chunked import, sortable/searchable imported-posts table, 4 bulk ops, row click → post editor). Gates green: backend `--plugin wp_import --full` (75 unit / 3 integration), fe-admin 715 Vitest + vue-tsc + ESLint. **Proven live against redrobot.online**: 10 posts imported with original dates, deduplicated cats+tags, REST-resolved featured images, plain-text excerpts; [HTML walkthrough, 5 screenshots](walkthrough/s81-WALK-REPORT-wp-import.html). On disk, not committed. Sprint: [done/s81](done/s81-import-from-word-press.md).
>
> **🟢 Recently done (2026-06-11):** **S66 ≡ S67.1 — backend device registration + APNs + meinchat push hook** — agnostic core mechanism (`vbwd_device_token` model + standalone core migration, `DeviceTokenService` idempotent upsert-by-token, ES256/HTTP-2 `ApnsClient` with sandbox flag + graceful no-op without a key, priority-ordered `push_dispatcher_registry`, `POST /api/v1/devices/register` + owner-only `DELETE /devices/<token>`) + gnostic `MeinChatPushHook` (preview vs `🔒` for `e2e_v1`, badge = inbox unread Σ, self-push suppressed, hook revokes on 410; `meinchat-plus` covered via a second per-app hook instance). `--full` gate green (3506 unit / 753 integration; 1 pre-existing unrelated ghrm config failure). The S67.1 wire contract is live — **S67.2 (iOS) is unblocked**. On disk, not committed. See report 05.
>
> **🟢 Recently done (2026-06-11):** **S65 (phase 1–2) — demo-data import to production** — authored hotel (5 categories / 15 resources) + doctor (12 nosologies / 28 clinics) booking envelopes and imported them through the **live admin Import/Export UI** on `hotel.vbwd.cc` + `doctor.vbwd.cc` (upsert, **0 errors**, verified via public `/api/v1/booking/resources`); manifests confirmed to expose the S61 exchangers before writing. Local + prod HTML walkthroughs with per-step screenshots under [`walkthrough/`](walkthrough/). Remaining S65: checkout proof (Stripe + tokens, ±coupon). See report 04.
>
> **🟢 Recently done (2026-06-11):** **S68 — CMS widget admin bugs** — the unified `cms_widgets` Import/Export now carries the full menu tree (`menu_items` export + idempotent `replace_tree` import, symmetric with the bespoke per-widget path), and widget deletion never 500s: in-use widgets (layout/page/post) get a 409 with usage counts, `?force=true` detaches and deletes (single + bulk, per-id results), IntegrityError backstop. `--plugin cms --full` green (548 unit / 160 integration), on disk, not committed. See report 03.
>
> **🟢 Recently done (2026-06-11):** **S70 — bot rich-choice rendering in meinchat + portable conversation styles** — the storefront bot now renders **styled cards / command menu / cart** in the real meinchat UI (priced choice cards, `/help` menu, `/cart`·`/cart-edit`·`/cart-clear`, tap-to-select), the bot-chat look is a **portable `BotConversationStyle`** exportable through the unified S46 Import/Export, and the fe applies the active style. 70.0–70.5 all gate-green on disk, **not committed**. Verified in the live UI; walkthrough rebuilt with real screenshots ([bot-base-meinchat-walk.html](../20260607/reports/bot-base-meinchat-walk.html)). See report 02.

## Theme

The S45 bot bridge + S53 storefront are now a **presentable in-chat commerce experience** (S70). Next rocks are the **Shopware-parity** follow-ups surfaced in report 01: the data-model foundations — **taxes/countries (S72)** and **user groups (S73)** — then the customer/catalog batch — **account type (S74)**, **shop address book + delivery (S75)**, **manufacturer/product-number/SEO (S76)**, a cross-cutting **tags + custom-fields framework (S77)**, and **cross-sell + product CMS layout (S78)** — plus the **iOS** counterpart of the bot rich rendering (S71).

## Sprints

| # | Sprint | Area | Status |
|---|--------|------|--------|
| **70** | [Bot rich-choice rendering in meinchat + portable conversation styles](../20260610/sprints/s70-bot-rich-choice-rendering-and-portable-styles.md) | `meinchat`/`bot_meinchat`/`bot_base`/`subscription` + `vbwd-fe-user` | 🟢 **DONE on disk, gate-green (70.0–70.5) — 2026-06-11**; not committed. Real-UI verified. |
| **71** | [iOS: meinchat + meinchat-plus updates for rich bot-choice rendering & styling](../20260610/sprints/s71-ios-meinchat-rich-choice-rendering.md) | meinchat iOS + meinchat-plus iOS | **PLANNED** — consumes the S70 `meta` contract; native cards/menu/cart + token→native theme; depends on 70.0 (green). |
| **68** | [CMS widget admin bugs: menu-item export + widget deletion](../20260610/done/s68-cms-widget-menu-export.md) | `plugins/cms` | 🟢 **DONE on disk, gate-green — 2026-06-11**; not committed. See [report 03](reports/03-s68-cms-widget-menu-export-and-deletion.md). |
| **69** | [Plan/add-on driven user permissions (`permissions_enable` / `special_permissions_enable`)](../20260610/done/s69-user-permissions-special-perms-subscription.md) | core (one RBAC write port) + `subscription` | 🟢 **DONE on disk — 2026-06-12**; DoD gate `--plugin subscription --full` green (144u/79i), core port 9/9; not committed. See [report 07](reports/07-s69-plan-addon-driven-permissions.md). **Core `--full` now green both ways** (CI/in-container + verbatim host script: lint + 3813 unit + 838 integration — [report 08](reports/08-core-full-gate-green.md); host-path enum-cascade fix committed `fda435d`). **fe-admin editor → S69.1.** |
| **66/67** | **Notifications & badges** — [S67 umbrella](../20260610/done/s67-notifications-and-badges.md): [S67.1 (BE) device-registration + APNs + push hook](../20260610/done/s67-1-be-device-registration-and-push.md) (**≡ [S66](../20260610/done/s66-backend-apns-push-notifications.md)** — canonical BE design, implemented once) · [S67.2 (iOS) app notifications + side-menu + app-icon badge](../20260610/done/s67-ios-notifications-and-badges.md) | `vbwd-backend` (core + `plugins/meinchat`) + `vbwd-ios` | 🟢 **DONE on disk, gates green — BE 2026-06-11 ([report 05](reports/05-s66-s67-1-backend-apns-push.md)) · iOS 2026-06-12 ([report 06](reports/06-s67-2-ios-notifications-and-badges.md))**; not committed. **Open: real-device APNs end-to-end proof** (device + portal Push capability + backend `.p8`). |
| **72** | [Taxes & Countries (umbrella)](done/s72-taxes.md) — [72.1 countries page](done/s72-1-tax-countries-page.md) · [72.2 import/export](done/s72-2-tax-import-export.md) · [72.3 entity assignment](done/s72-3-entity-tax-assignment.md) · [72.4 netto/brutto](done/s72-4-netto-brutto-display.md) | core + `vbwd-fe-admin` + `subscription`/`shop`/`booking` + `vbwd-fe-user` | 🟢 **DONE on disk, all 4 sub-sprints gate-green — 2026-06-12**; not committed. Dev DB migrated; override verified live. S72.5 (multi-tax line splitting) deferred. |
| **73** | [User Groups (new core entity) + subscription-driven membership](sprints/s73-user-groups.md) | core + `vbwd-fe-admin` + `subscription` | **PLANNED** |
| **74** | [User account type (private person / business)](sprints/s74-user-account-type.md) | core + `vbwd-fe-admin` + fe-user profile | **PLANNED** — confirms core keeps a single billing address (multi-address is shop-only, S75). |
| **75** | [Shop address book & delivery address](sprints/s75-shop-address-book-delivery.md) | `shop` + `vbwd-fe-user` + `vbwd-fe-admin` | **PLANNED** — shop-owned `shop_user_address` (multiple, default delivery); checkout save-or-ephemeral → `Order.shipping_address` snapshot; orders carry immutable snapshots. |
| **76** | [Shop catalog parity: manufacturer, product number/EAN, SEO](sprints/s76-shop-catalog-parity.md) | `shop` + `shop-admin` + `vbwd-fe-user` | **PLANNED** — `Manufacturer` model + FK; distinct `product_number` (auto) + `ean`; SEO reusing the CMS S56 shape. |
| **77** | [Generic Tags & Custom Fields framework (BE + FE)](sprints/s77-tags-custom-fields-framework.md) | **core** + `vbwd-fe-admin` + consumers (`user`/`shop_product`) | **PLANNED** — polymorphic `(entity_type, entity_id)` tags + typed custom fields (no per-entity migration); core port + reusable `TagPicker`/`CustomFieldsEditor`; json/csv. |
| **78** | [Shop cross-selling + product CMS layout](sprints/s78-shop-crosssell-cms-layout.md) | `shop` + `shop-admin` + `vbwd-fe-user` | **PLANNED** — typed `shop_product_crosssell`; `cms_layout_slug` rendered via CMS `CmsLayoutRenderer` (graceful fallback when CMS off). |
| **79** | [Withdraw (token balance → money payout)](sprints/s79-withdraw.md) | NEW `withdraw` plugin duo + `stripe`/`paypal`/`truemoney`/`promptpay` | **PLANNED** — `PayoutProvider` capability ABC in core plugin infra; `IWithdrawableBalance` seam (S80 reuses); schema-driven destination forms; admin approval + refund-on-failure. |
| **80** | [GHRM marketplace (vendors publish, sell, withdraw)](sprints/s80-ghrm-market-place.md) | NEW `ghrm_marketplace` plugin duo (deps: ghrm, subscription, S79 withdraw) | **PLANNED** — append-only vendor ledger (SALE/SALE_REVERSAL/WITHDRAW); admin-reviewed listings → plan+package on publish; "Software" nav group (needs generic group rendering, D6). Depends on S79. |
| **81** | [Import from WordPress (RSS → CMS posts)](done/s81-import-from-word-press.md) | NEW `wp_import` (BE) + `wp-import` (fe-admin) plugin duo | 🟢 **DONE on disk, gate-green — 2026-06-12**; not committed. Live-proven vs redrobot.online; [walkthrough](walkthrough/s81-WALK-REPORT-wp-import.html). |
| **82** | [Video hosting (CMS "video" post type: upload + embed)](sprints/s82-video-hosting.md) | `plugins/cms` + `cms-admin` + fe-user `cms` | **PLANNED** — registered `video` post type (no `cms_post` migration); managed `CmsVideo` asset mirroring `cms_images` (range-served, ZIP-exchangeable); HTML5 player / sanitised embed via the layout renderer; reuses `cms.manage`. |
| **83** | [Content paywall (pay-per-view with tokens; `<vbwdpaywall>` tag)](sprints/s83-content-paywall.md) | NEW `paywall` plugin trio (BE + fe-admin + fe-user; deps: `cms` + core `TokenService`) | **PLANNED** — `<vbwdpaywall id=…/>` gates content **server-side** via a new generic `cms_public_content_filter_registry` (cms stays paywall-agnostic); instant core-token debit, permanent double-charge-proof `PaywallAccess`; Sales → Paywall two-tab admin (paywalls + tarif-plan-style categories). **Open:** subscription auto-unlock deferred (D10 flag, default off). |

## Carry-forward / open

- **Bot epic packaging** — batch-create + push the 4 standalone `vbwd-plugin-*` repos (`bot-base`, `bot-telegram`, `bot-meinchat`, `fe-admin-bot-telegram`); still pending the owner's go-ahead (irreversible outward action).
- **45.5.1** — meinchat E2E bot-participant decrypt, blocked on meinchat-plus implementing `e2e_v1` Signal decrypt; S70 rich `meta` rides the **plain** path; S71 reserves the `{body,meta}` envelope shape.
- **Real Telegram smoke test (S45 Q2)** — ready whenever a @BotFather token is provided.
- **S67 real-device APNs proof** — acceptance §6 of [S67.2](../20260610/done/s67-ios-notifications-and-badges.md): physical device + Apple-portal Push capability on the provisioning profile + `.p8` key mounted on the backend (sandbox first). Code on both ends is gate-green; only the live wire is unproven.
- Optional **fe-admin styling UI** for `BotConversationStyle` (today: generic plugin config + Import/Export surface).

## Dev-stack note (reversible, 2026-06-11)

The storefront catalog is deactivated to the demo set (`is_active` toggle: Starter/Pro/Business · Priority Support/Extra Seats · 1k/5k/20k); `bot-meinchat` + `subscription.bot_storefront_enabled` on; a `Default` `BotConversationStyle` seeded; bot user `assistant` provisioned; S70 migrations applied to the dev DB. Plugins are gitignored/on-disk — nothing committed.
