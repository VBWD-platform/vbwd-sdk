the problem: freshli imported pages have no layout. 
the task - add to the bulk operation in post/page bulk assignt to a template
default template shall be defined in cms backend plugin settings
if no default layout by page/post then read the default from backend plugin and use it 
