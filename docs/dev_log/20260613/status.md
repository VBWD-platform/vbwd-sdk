# 2026-06-13 — Dev day status

> Forward backlog. Planned/unfinished sprints carried from [`../20260611/status.md`](../20260611/status.md) and earlier; everything **done** stays archived under its dev-day `done/`.
> **Reports (this day):** [02 — pricing-model audit](reports/02-pricing-model-audit.md) · [09 — S73/S74 user groups + account type + role lookup](reports/09-s73-s74-user-groups-account-type-and-role-lookup.md) · [10 — TaxClass flatten + tax-delete 409](reports/10-taxclass-flatten-and-tax-delete-409.md) · [11 — S86 rooms + bot-widget](reports/11-s86-rooms-and-bot-widget.md).
>
> **🟢 Recently done (2026-06-13):** **S73 — User Groups**, **S74 — User account type**, and an ad-hoc **User-Role lookup table** — all gate-green on disk (not committed). **S73:** new core `UserGroup` (`vbwd_user_groups`) + slug-keyed membership (`vbwd_user_group_rel`); CRUD + bulk + json/csv import/export at `/admin/settings/user_groups` (nav under Access Levels) + a `DualListSelector` Groups block on user-edit; subscription drives **managed** memberships via reconcile (D4: check-out wins, overlap-safe, un-managed untouched) reaching groups only through the core `IUserGroupMembership` port. Migration `20260611_1000_user_groups`. **S74:** `account_type` (private/business, default private, business-requires-company) on `UserDetails` with validation on all admin+self-service write paths, invoice party rendering (company+VAT vs person), and the select on fe-admin `UserEdit` + fe-user `Profile`. Migration `20260613_1400_user_account_type`. **Role lookup:** `User.role` native PG enum → varchar **FK** into a real `vbwd_user_role` lookup table (`RoleDefinition`, capitalised-slug PK + name, 6 canonical rows seeded via migration **and** an `after_create` event for `create_all`/test DBs); native `userrole` type dropped; all `user.role == UserRole.X` comparisons + wire contract preserved via `db.Enum(native_enum=False)`. Migration `20260613_1500_user_role_lookup` (current single core head). **Naming note:** the lookup table is singular `vbwd_user_role` because plural `vbwd_user_roles` is the existing RBAC join table (left untouched — see report 09). Gates green per scope (core `--full` lint+unit+integration; fe-admin/fe-user Vitest + lint). See [report 09](reports/09-s73-s74-user-groups-account-type-and-role-lookup.md). Sprints: [done/s73](done/s73-user-groups.md) · [done/s74](done/s74-user-account-type.md).
>
> **🟢 Recently done (2026-06-13):** **TaxClass flattened → `tax_class` field (+ in-use tax-delete 409 fix)** — `TaxClass` was wired but functionally inert (nothing reads it for pricing/assignment; entities link specific taxes by `tax_id` since S72.3). Flattened it into a denormalized `tax_class VARCHAR` on `Tax` (mirrors legacy `shop_product.tax_class`): migration `20260613_1100_flatten_tax_cls` drops `tax_class_id` + the `vbwd_tax_class` table (data-migrated to the class `code`); `TaxClass` model + class CRUD routes (now 404) + the `tax_classes` exchanger removed; `taxes` exchanger now carries `tax_class` as a plain field. fe-admin: Tax Classes section gone, a `tax_class` field on the rate form, one `taxes` import/export. **Bonus:** the core tax-delete route now catches the S72.3 `ON DELETE RESTRICT` FK violation → **409 "in use"** instead of a 500 (regression test added) — fixing bulk tax delete. Gates green: backend core (67 scoped + tax-route 17 incl. 409 regression; 1532 unit), fe-admin **855 Vitest** + lint + vue-tsc. **Alembic heads merged** — the flatten branch was unified with the S85 `drop_price` branch by the S77 merge revision `20260613_1200_tags_custom_fields` (`flatten → user_groups → merge ← drop_price`); core is single-headed at `20260614_1000_inv_line_tax_bd` (the other heads are the by-design per-plugin branches). On disk, not committed. Sprint: [done/s84.1](done/s84.1-taxclass-flatten.md) · Report: [10](reports/10-taxclass-flatten-and-tax-delete-409.md).
>
> **🟢 Recently done (2026-06-13):** **S84 — Currency admin tab** — a new **Currency** sub-tab on `/admin/settings/tax-and-countries` with four blocks: active/available two-panel selector + search, default-currency selector, default-vs-USD cross-rate base switcher, and an editable rate sheet (one general "Save rates"). **Storage:** active set + default + base live in the **core settings JSON** (single source of truth); `vbwd_currency` is catalog + rates only — the `is_active`/`is_default` columns were **dropped** and `exchange_rate` widened to `Numeric(18,8)` (core migration). `CurrencyService` is settings-sourced; `set_default` re-bases all rates (Decimal, value-preserving); **conversions are Decimal end-to-end, accurate ≤1e-7** across any base. Admin routes for list/activate/deactivate/set-default/rate + **create/delete catalog currencies** (delete blocked for active/default); one `currencies` bulk import/export. Gates green: core Part A/B + currency integration (71 unit + create/delete), fe-admin **850 Vitest** + lint + vue-tsc. **Live-verified** with an 8-step [walkthrough](walkthrough/s84-WALK-REPORT-currency.html) (both rate bases, default re-base, add+delete flow). Side-fixes: re-added the missing `nav.taxesAndCountries` translation (8 locales) + fixed a build-blocking `vue-tsc` error in an unrelated cms-admin spec. On disk, not committed. Sprint: [done/s84](done/s84-currency-admin-tab.md).

---

## Theme

The work is a broad **Shopware-parity + platform-breadth** push, and the day closed two core rocks: **S84 (currency)** landed gate-green and live-verified, while **S77 (generic Tags & Custom Fields)** — the framework the rest of the catalog leans on — has its core slice done on disk (models, port, registry, migration) with admin routes, the fe components, and the per-entity wiring still ahead. Around them a deep planned backlog fans out: account/address/catalog parity (S73–S76, S78), the withdraw → GHRM-marketplace chain (S79–S80), content surfaces (S82 video, S83 paywall), and the meinchat **rooms / bot-widget** umbrella (S86). Newly drafted today: **S41 — CMS AI Helper**, introducing **LoopForge**, a clean import-free pipeline engine, with manifest-driven fields that opt into S77 custom fields. **Next rock:** finish S77's consumer-facing slices (it unblocks the most downstream sprints), then settle the S41 negotiation.

## Sprints

| # | Sprint | Area | Status |
|---|--------|------|--------|
| **70** | [Bot rich-choice rendering in meinchat + portable conversation styles](../20260610/sprints/s70-bot-rich-choice-rendering-and-portable-styles.md) | `meinchat`/`bot_meinchat`/`bot_base`/`subscription` + `vbwd-fe-user` | 🟢 **DONE on disk, gate-green (70.0–70.5) — 2026-06-11**; not committed. Real-UI verified. |
| **71** | [iOS: meinchat + meinchat-plus updates for rich bot-choice rendering & styling](../20260610/sprints/s71-ios-meinchat-rich-choice-rendering.md) | meinchat iOS + meinchat-plus iOS | **PLANNED** — consumes the S70 `meta` contract; native cards/menu/cart + token→native theme; depends on 70.0 (green). |
| **68** | [CMS widget admin bugs: menu-item export + widget deletion](../20260610/done/s68-cms-widget-menu-export.md) | `plugins/cms` | 🟢 **DONE on disk, gate-green — 2026-06-11**; not committed. See [report 03](reports/03-s68-cms-widget-menu-export-and-deletion.md). |
| **69** | [Plan/add-on driven user permissions (`permissions_enable` / `special_permissions_enable`)](../20260610/done/s69-user-permissions-special-perms-subscription.md) | core (one RBAC write port) + `subscription` | 🟢 **DONE on disk — 2026-06-12**; DoD gate `--plugin subscription --full` green (144u/79i), core port 9/9; not committed. See [report 07](reports/07-s69-plan-addon-driven-permissions.md). **Core `--full` now green both ways** (CI/in-container + verbatim host script: lint + 3813 unit + 838 integration — [report 08](reports/08-core-full-gate-green.md); host-path enum-cascade fix committed `fda435d`). **fe-admin editor → S69.1.** |
| **66/67** | **Notifications & badges** — [S67 umbrella](../20260610/done/s67-notifications-and-badges.md): [S67.1 (BE) device-registration + APNs + push hook](../20260610/done/s67-1-be-device-registration-and-push.md) (**≡ [S66](../20260610/done/s66-backend-apns-push-notifications.md)** — canonical BE design, implemented once) · [S67.2 (iOS) app notifications + side-menu + app-icon badge](../20260610/done/s67-ios-notifications-and-badges.md) | `vbwd-backend` (core + `plugins/meinchat`) + `vbwd-ios` | 🟢 **DONE on disk, gates green — BE 2026-06-11 ([report 05](reports/05-s66-s67-1-backend-apns-push.md)) · iOS 2026-06-12 ([report 06](reports/06-s67-2-ios-notifications-and-badges.md))**; not committed. **Open: real-device APNs end-to-end proof** (device + portal Push capability + backend `.p8`). |
| **72** | [Taxes & Countries (umbrella)](done/s72-taxes.md) — [72.1 countries page](done/s72-1-tax-countries-page.md) · [72.2 import/export](done/s72-2-tax-import-export.md) · [72.3 entity assignment](done/s72-3-entity-tax-assignment.md) · [72.4 netto/brutto](done/s72-4-netto-brutto-display.md) | core + `vbwd-fe-admin` + `subscription`/`shop`/`booking` + `vbwd-fe-user` | 🟢 **DONE on disk, all 4 sub-sprints gate-green — 2026-06-12**; not committed. Dev DB migrated; override verified live. S72.5 (multi-tax line splitting) deferred. **2026-06-13 follow-ups ([report 10](reports/10-taxclass-flatten-and-tax-delete-409.md)):** `TaxClass` flattened into a `tax_class` field on `Tax` (model/table/exchanger/CRUD removed); the S72.3 in-use-tax-delete **409** finally wired (was a 500). |
| **73** | [User Groups (new core entity) + subscription-driven membership](done/s73-user-groups.md) | core + `vbwd-fe-admin` + `subscription` | 🟢 **DONE on disk, gate-green — 2026-06-13**; not committed. `vbwd_user_groups` + slug-keyed `vbwd_user_group_rel`; admin list/bulk/import-export + nav under Access Levels + user-edit selector; subscription reconcile via `IUserGroupMembership` port (D4: check-out wins, un-managed untouched). Migration `20260611_1000_user_groups`. See [report 09](reports/09-s73-s74-user-groups-account-type-and-role-lookup.md). |
| **74** | [User account type (private person / business)](done/s74-user-account-type.md) | core + `vbwd-fe-admin` + fe-user profile | 🟢 **DONE on disk, gate-green — 2026-06-13**; not committed. `account_type` on `UserDetails` (default private, business-requires-company), invoice party rendering (company+VAT vs person), select on fe-admin UserEdit + fe-user Profile. Core keeps a single billing address (multi-address is shop-only, S75). Migration `20260613_1400_user_account_type`. See [report 09](reports/09-s73-s74-user-groups-account-type-and-role-lookup.md). |
| **75** | [Shop address book & delivery address](sprints/s75-shop-address-book-delivery.md) | `shop` + `vbwd-fe-user` + `vbwd-fe-admin` | **PLANNED** — shop-owned `shop_user_address` (multiple, default delivery); checkout save-or-ephemeral → `Order.shipping_address` snapshot; orders carry immutable snapshots. |
| **76** | [Shop catalog parity: manufacturer, product number/EAN, SEO](sprints/s76-shop-catalog-parity.md) | `shop` + `shop-admin` + `vbwd-fe-user` | **PLANNED** — `Manufacturer` model + FK; distinct `product_number` (auto) + `ean`; SEO reusing the CMS S56 shape. |
| **77** | [Generic Tags & Custom Fields framework (BE + FE)](sprints/s77-tags-custom-fields-framework.md) | **core** + `vbwd-fe-core`/`vbwd-fe-admin` + all consumers (`user`/`shop_product`/`tarif_plan`/`addon`/`booking_resource`/`ghrm_software_package`/`cms_page`/`cms_post`/`invoice_line_item`) | 🟡 **IN PROGRESS — core slice DONE on disk, gate-green 2026-06-13** (not committed). Built: 4 models (`vbwd_tag`/`vbwd_entity_tag`/`vbwd_custom_field_def`/`vbwd_custom_field_value`) + repos + `TagService`/`CustomFieldService` + `entity_type_registry` + `ITagsAndCustomFields` port/default-impl/container wiring + core migration `20260613_1200_tags_cf` (merges the user-groups + drop-price heads → single core head, validated up/down/up). Core `--lint` + **1599 unit + 183 integration** green; **22** S77 unit + agnosticism oracle + **12** S77 integration green; no `plugins.*` import. **Slice 2 DONE on disk, gate-green 2026-06-13** (not committed): core **admin routes** (`vbwd/routes/admin/tags_custom_fields.py` — tag catalog CRUD + bulk-delete with `get_stats_bulk` **stats** column, custom-field-def CRUD + filters, `/custom-field-entity-types`, generic `GET|PUT /admin/<entity_type>/<id>/{tags,custom-fields}` → 404 if type unregistered else enforce its own `manage_permission`, 400 on validation) + **data-exchange** `tags` (natural key `slug`) + `custom_field_defs` (composite `(entity_type,key)`) exchangers (json+csv, idempotent) auto-wired into the generic exchange routes; **27** slice-2 tests green. **Slice 3 (frontend) DONE on disk, gate-green 2026-06-13** (not committed): **fe-core** `TagChips`/`CustomFieldsDisplay` (built once, exported, dist rebuilt + submodules synced); **fe-admin** entity-agnostic `TagPicker`/`CustomFieldsEditor` + the **Settings → "Custom Fields"** two-tab catalog page (Custom-fields + Global-tags: filters, quick-search, bulk ops, import/export, stats column) + nav entry; wired edit pages **`user`/`shop_product`/`tarif_plan`**; **fe-user** product + tarif cards mount the display components; backend `on_enable` registers `shop_product`/`tarif_plan`/`addon` + shop product-detail serializer appends tags/CF. Gates: fe-core build+lint + 11 specs, fe-admin **893** Vitest + lint + vue-tsc, fe-user **930** Vitest + lint + vue-tsc, shop/subscription `--plugin --full` green. **Slice 4 (finish-consumers) DONE on disk, gate-green 2026-06-14** (not committed): all remaining entities wired the same way — `tarif_plan` serializer-append (card now non-empty), `addon` (register+serializer+`AddonForm.vue`+`AddonInfoView.vue`), `booking_resource` (booking `on_enable`+routes+`ResourceForm.vue`+`BookingResourceDetail.vue`), `ghrm_software_package` (ghrm `on_enable`+routes+`GhrmSoftwareTab.vue`+`GhrmPackageDetail.vue`), `cms_page`/`cms_post` (cms `on_enable`+routes keyed by type+`PostEditor.vue`+`CmsPageTypeBase.vue`, NEW core tags/CF strictly ALONGSIDE existing `cms_term` taxonomy — untouched). **8/9 entity types LIVE** (registered+serializer+edit-page+card). Gates: subscription/cms/booking `--plugin --full` green, ghrm lint+unit+S77 green, fe-admin **937** + fe-user **1010** Vitest + lint + vue-tsc, agnosticism oracles green. One core-tracked fix: `vbwd/testing/integration_db.py` re-seeds canonical `vbwd_user_role` after TRUNCATE (agnostic). List `*_bulk` (D6) **bounded** — no catalogue card shows tags yet, so no dead per-page query added. **⚠ Pre-existing, NOT S77:** GHRM `--full` integration RED on stale `price_float=` (TarifPlan rename never propagated to 6 ghrm test files — mechanical `price_float`→`price` rename pending). **Slice 5 (invoice snapshot) DONE on disk, gate-green 2026-06-14** (not committed): `invoice_line_item` registered (core, `invoices.manage`); new core `vbwd/services/invoice_line_item_snapshot.py` + `ILineItemHandler.resolve_catalog_entity_ref` (plugin handlers resolve source `(entity_type,id)`; core orchestrates the copy → no `from plugins.*`) **freezes** source tags/CF onto the line at creation; `UserInvoice.to_dict()` appends per-line frozen tags/CF via `get_*_bulk` (one query/invoice); fe-admin `InvoiceDetails.vue` + fe-user `InvoiceDetail.vue` render them; **immutability tested**. **→ 9/9 entity types LIVE.** **Slice 6 (core-style consistency) DONE on disk, gate-green 2026-06-14** (not committed): S77 fe-admin UI now matches common core styles — `TagPicker`/`CustomFieldsEditor`/`CustomFieldsSettings` use fe-core `<Button>` (dead unstyled `.btn` classes removed) + token-based (`var(--vbwd-*)`) inputs/selects + Settings chrome aligned to sibling tabs; testids/i18n preserved; fe-admin **942** Vitest + lint + vue-tsc green. **Slice 7 (block separation) DONE on disk, gate-green 2026-06-14** (not committed): user-reported "blocks too close, no dividers" — `TagPicker`/`CustomFieldsEditor` now each render as a self-contained **`.form-section` card** (token border/radius/padding + `margin-bottom`, `<h3>` heading) matching the currency-tab gold standard, so all 8 host edit pages get separated, harmonious blocks (DRY, no per-host CSS); booking host wrapper got top spacing; fe-admin **945** Vitest + lint + vue-tsc green; **live screenshot proof** at `walkthrough/s77-after-product-edit.png` (+ currency-tab reference). **D7 DONE on disk, gate-green 2026-06-14** (not committed): CMS tags folded into core — `tag` term type deregistered (TermManager Tag tab gone), content-ingest + wp-import `tags[]` → `set_tags("cms_post",…)`, post-by-tag read path → core `entity_tag`; CMS-plugin migration `20260613_1300_fold_tags` (merge node on prior cms head + core `tags_cf`) reimports `cms_term('tag')`→`vbwd_tag` (`parent_entity_type='cms_post'`)+links→`entity_tag`, count-verifies, drops old tag rows/links; **`cms_term` now categories-only**, categories untouched; up/down/up validated; `--plugin cms --full` (188 integ) + `--plugin wp_import --full` green; fe-admin 943. (One tag read-surface left on old join: FTS `search_repository` — flagged for follow-up.) **Next/final:** **HTML walkthrough** (DoD live-proof). Polymorphic `(entity_type, entity_id)` tags + typed custom fields, no per-entity migration. Locked: narrow `ITagsAndCustomFields` port + `EntityTypeRegistration` registry (mirror `entitlement.py`); **D5** names it EAV and bounds it to the by-id path (no cross-catalog filtering — deferred read projection, per WordPress/Magento/Amazon); **D6** bulk reads (no N+1) + cached defs + set-based import → scales to 1M SKUs. Tags gain `parent_entity_type` (nullable=global) + `meta_data` + usage **stats** (`get_stats_bulk`). Wiring: fe-core read-only `TagChips`/`CustomFieldsDisplay` (cards/detail/invoice), fe-admin `TagPicker`/`CustomFieldsEditor` on **every** edit page, **Settings → "Custom Fields"** catalog page (Custom-fields + Global-tags tabs: filters, quick-search, bulk ops, import/export, tag stats column). Invoices **snapshot** source CF/tags onto `invoice_line_item`. **D7 (migration):** tags→core, categories stay plugin-owned; audit found **CMS is the sole tag owner** (`cms_term('tag')`+`cms_post_term`) — reimport into `vbwd_tag`/`entity_tag`, drop old tag rows; shop/booking/subscription/ghrm have categories only (untouched). |
| **78** | [Shop cross-selling + product CMS layout](sprints/s78-shop-crosssell-cms-layout.md) | `shop` + `shop-admin` + `vbwd-fe-user` | **PLANNED** — typed `shop_product_crosssell`; `cms_layout_slug` rendered via CMS `CmsLayoutRenderer` (graceful fallback when CMS off). |
| **79** | [Withdraw (token balance → money payout)](sprints/s79-withdraw.md) | NEW `withdraw` plugin duo + `stripe`/`paypal`/`truemoney`/`promptpay` | **PLANNED** — `PayoutProvider` capability ABC in core plugin infra; `IWithdrawableBalance` seam (S80 reuses); schema-driven destination forms; admin approval + refund-on-failure. |
| **80** | [GHRM marketplace (vendors publish, sell, withdraw)](sprints/s80-ghrm-market-place.md) | NEW `ghrm_marketplace` plugin duo (deps: ghrm, subscription, S79 withdraw) | **PLANNED** — append-only vendor ledger (SALE/SALE_REVERSAL/WITHDRAW); admin-reviewed listings → plan+package on publish; "Software" nav group (needs generic group rendering, D6). Depends on S79. |
| **81** | [Import from WordPress (RSS → CMS posts)](done/s81-import-from-word-press.md) | NEW `wp_import` (BE) + `wp-import` (fe-admin) plugin duo | 🟢 **DONE on disk, gate-green — 2026-06-12**; not committed. Live-proven vs redrobot.online; [walkthrough](walkthrough/s81-WALK-REPORT-wp-import.html). |
| **82** | [Video hosting (CMS "video" post type: upload + embed)](sprints/s82-video-hosting.md) | `plugins/cms` + `cms-admin` + fe-user `cms` | **PLANNED** — registered `video` post type (no `cms_post` migration); managed `CmsVideo` asset mirroring `cms_images` (range-served, ZIP-exchangeable); HTML5 player / sanitised embed via the layout renderer; reuses `cms.manage`. |
| **83** | [Content paywall (pay-per-view with tokens; `<vbwdpaywall>` tag)](sprints/s83-content-paywall.md) | NEW `paywall` plugin trio (BE + fe-admin + fe-user; deps: `cms` + core `TokenService`) | **PLANNED** — `<vbwdpaywall id=…/>` gates content **server-side** via a new generic `cms_public_content_filter_registry` (cms stays paywall-agnostic); instant core-token debit, permanent double-charge-proof `PaywallAccess`; Sales → Paywall two-tab admin (paywalls + tarif-plan-style categories). **Open:** subscription auto-unlock deferred (D10 flag, default off). |
| **84** | [Currency admin tab (active/available, default, cross-rate base, editable rate sheet)](done/s84-currency-admin-tab.md) | **core** (`Currency` + settings + admin routes) + `vbwd-fe-admin` | 🟢 **DONE on disk, gate-green — 2026-06-13**; not committed. Live-verified ([walkthrough](walkthrough/s84-WALK-REPORT-currency.html), 8 steps). Active/default/base in core settings JSON; catalog+rates on `vbwd_currency` (`is_active`/`is_default` columns dropped, `exchange_rate`→`Numeric(18,8)`); Decimal conversions ≤1e-7; one bulk import/export; single Save; add/delete catalog currencies. |
| **41** | [CMS AI Helper: LoopForge-driven content & SEO generation in the editor](sprints/s41-cms-ai-helper.md) | `vbwd-backend` (`cms-ai` plugin + **LoopForge** engine) + `vbwd-fe-admin` (`cms-ai` fe plugin + `cms-admin` seams) | 🟠 **DRAFT for negotiation — rewritten 2026-06-13.** `AI ✨` dropdown + collapsible prompt panel on `PostEditor.vue` (pages **and** posts); backend-proxied generate (key never in browser) → **LoopForge**, a fresh import-clean (no Flask/SQLAlchemy/session) multi-step pipeline engine at `plugins/cms-ai/loopforge/` (loopai = concept ref only; pip/submodule-ready). **One plugin = `cms-ai`** (no separate cms-ai-helper). **Manifest-driven fields:** admin-editable `template-1.json`/`loopforge-flow-1.yaml`/`template-1-vars.json` under `${VBWD_VAR_DIR}/assets/cms-ai/prompts/` (via `asset_storage.py`) declare request-context + response fields — add/remove without code; **S77 custom fields** opt-in via the manifest. **Stateless generate → patch**; FE fills form + S77 CF inputs; persists **only on Save** (S77 owns CF persistence; soft dep declared). LLM adapters use the **official OpenAI + Anthropic SDKs**, provider chosen **by model name** (`claude-*` → Anthropic); JSON-mode + repair/retry. **Image generation (P1):** a **"Generate only image from the prompt"** checkbox → LoopForge `ReplicateImageAdapter` (**Replicate / Black Forest FLUX** `flux-schnell`) → **uploaded to the global CMS image gallery immediately** (reuses `CmsImageService`; declared `cms` dep) → `<img>` appended to `content_html`. AI button/panel **reuse fe-core design system** (no bespoke CSS). **DoD:** `--full` green both repos + LoopForge standalone suite; **Playwright HTML walkthrough** with the **backlinko live test** + an **image-gen → gallery** step (side-by-side proof-of-work, not pixel-diff); **developer docs (md + html, screenshots)**. P2 (file uploads *into* the model / "style like this") specified, deferred. |
| **87** | [Cookie / GDPR-DSGVO consent popup](sprints/s87-cookie-consent-gdpr-popup.md) | **NEW standalone repo** `vbwd-fe-user-plugin-cookie-consent` (fe-user plugin only) | 🟠 **DRAFT for negotiation — 2026-06-13.** Site-wide consent overlay on the public storefront with exactly **Accept all** / **Reject all**; **reject ⇒ no login, no buy** (a plugin router guard via `sdk.addRouterGuard` gates `/login` + `/checkout*` route names/prefixes; relax toggle `block_auth_and_commerce_on_reject` default on). **Self-mounted Teleport-to-body overlay** (App.vue has no plugin slot, `GlobalWidgets` retired) → **zero core/backend/fe-admin/other-plugin change**. Consent state in `localStorage` (versioned, re-prompt on bump); persistent **"Cookie settings"** re-open so a rejector can later accept. Named-export plugin, **own repo** (mirrors `vbwd-fe-user-plugin-cms-youtube`); popup reuses **fe-core design system** (`var(--vbwd-*)`, theme/mobile); 8 locales; `config.json`+`admin-config.json`. **DoD:** fe-user gate green (Vitest+ESLint+vue-tsc) + **Playwright HTML walkthrough** (first-visit → reject blocks login/buy → settings→accept unblocks → persists). Out: granular categories, server-side consent log, script-gating. |
| **89** | [Heavy-load data-exchange: CLI · UI · API import/export speed at 100k→1M](sprints/s89-heavy-load-data-exchange.md) | `vbwd-platform` (load profile + bench + thresholds) + `vbwd-backend` (configurable row-cap, streaming export, bulk test-data CLI) | 🟠 **DRAFT for negotiation — 2026-06-14.** Continuation of the S29/S30 heavy-load line: a new `data-exchange` load profile measuring export+import across **CLI, API, fe-admin UI** for `shop_products`/`bookings`/`subscription_plans`/`subscription_addons` at two tiers this sprint (T1 100k+10k×3, T2 100k×4; **T3 1M×4 deferred** — capacity decision). Each cell carries a **bottleneck-attribution** breakdown (DB read/write/commit time, query count + rows/query for N+1, serialise vs transport vs overhead, GC pauses, longest txn/lock-wait, app vs DB CPU, table/index size, proxy-cut/body-size flags) rendered as a stacked stage chart — so a slow cell names *which stage* dominates, not just that it's slow. Backend blockers it must clear: `DEFAULT_ROW_CAP=10000` (→ env-configurable), in-memory export materialisation (→ streamed/chunked, **NDJSON** artefact + streaming import), a `flask data-exchange bulk-seed` generator (through the repo layer, never raw SQL), and a load-env-gated server-side stage-timing collector (SQLAlchemy hook → `Server-Timing`/`_profile`). T1 is the CI gate; T2 is a manual cost-gated sweep. **Decisions (2026-06-14):** cap at T2, NDJSON streaming both ends, keep the server-side timing collector. **Out:** T3/1M (deferred — runner capacity), legacy-JSON streaming import, prod nginx tuning, concurrency — measured-as-findings, not pre-fixed. |
| **86** | **[meinchat Rooms (multi-party) + bot-widget as a room — UMBRELLA](sprints/s86-bot-widget.md)** — [86.1 rooms foundation + core GUEST](sprints/s86-1-rooms-foundation.md) · [86.2 E2E rooms (fan-out)](sprints/s86-2-rooms-e2e.md) · [86.3 bot-widget as a room](sprints/s86-3-bot-widget-room.md) | **core** (`UserRole.GUEST`) + `meinchat` (rooms) + `meinchat_plus` (E2E) + `bot_meinchat` (bots as members) + fe-user/fe-admin meinchat(-plus/-admin) + `cms-admin` (shared editor seam) | **PLANNED** — N-party **rooms** in base meinchat (creator=admin, **any member can invite**, **only members read**); **encrypted unless invited** via meinchat-plus per-recipient **fan-out** over existing pairwise Signal sessions (no new group-crypto; new members see future msgs only). **Any BOT member ⇒ plain** room. The **bot-widget is a room** auto-created on **"Start Conversation"** with the editor's configured member nicknames (bots allowed: checkout + LLM bot); visitor = admin; public visitor = core **GUEST** (public-scope, no login). Config server-trusted via a narrow declared `cms` port. Multi-bot routing: free text → LLM bot, `/command` → owning bot (D7, confirmed). **🟢 S86.1 DONE on disk (2026-06-13)** — Slice 0 core `GUEST` (enum + login guard + standalone enum-add migration; 7u+migration green), Slice 1a rooms domain (`meinchat_room`/`meinchat_room_member` + `meinchat_message.room_id` w/ exactly-one-parent CHECK + `RoomService` + protocol seam), Slice 1b routes/room-message-path/one-stream SSE (`subscribe_many`)/retention, Slice 2 fe-user UI (`useRoomsStore`, `RoomView`, create/invite/remove, inbox rows). Gates green: `--plugin meinchat --full` + `--plugin meinchat_plus --full`; fe-user Vitest 925 / ESLint 0 / vue-tsc 0. Slice 3 (fe-admin) = no-op (privacy: no content inspection). **Caveat:** GUEST core changes green, but whole-core `--full` has pre-existing unrelated reds (concurrent S74 lint + migration-graph fragmentation). Rename UI deferred (no backend route). Not committed. **🟢 S86.2a DONE (2026-06-13)** — backend E2E for rooms: per-recipient **fan-out** over the existing 1:1 CBOR per-device envelope (no new crypto), `IRoomPolicy`/`IRoomCapabilities` ports, `AllMembersHaveDeviceKeys` (vetoes `member_has_no_device_keys`/`bot_member_cannot_e2e` instead of silently downgrading), forward-only "encrypted unless invited" (D4); `--plugin meinchat_plus --full` + `--plugin meinchat --full` green. **86.2b (fe-user crypto fan-out) deferred** by owner (widget rooms are plain → not blocking). **🟢 S86.3 DONE (2026-06-14)** — bot-widget-as-a-room: 3a `widget/start` (public+logged-in) + `GUEST` provisioning + soft-dep `ICmsWidgetReader` + `meinchat_guest_session`; 3b bots reply inside rooms (`"room:<id>"` opaque chat_id, post-send hooks now fire on room sends, D7 per-room routing free); 3c token economy (grant on activation, atomic per-round charge at send → **402 `insufficient_tokens`**, balance surfaced) + L1 overuse protection (cookie+localStorage guest reuse, no re-grant; fingerprint **file-log** only); 3d fe-admin editor (cms-admin shared widget-editor seam + meinchat-admin `MeinchatChatWidgetEditorTab`); 3e fe-user `MeinchatChatWidget` (gates, name/nickname prompt, Start, guest-scoped auth, **Buy tokens** block → `/dashboard/plans`). Gates green: `--plugin meinchat --full`, `--plugin bot_meinchat --full`, fe-admin Vitest 905/ESLint 0/vue-tsc 0, fe-user Vitest 964/ESLint 0/vue-tsc 0. **Adds a small core DRY method** `AuthService.generate_access_token`. Not committed. **Billing REVISED → 1 token = 1 word** (gate: balance>0; post-send hook charges question+answer words; buy-block → `/tokens` placeholder). **🟢 LIVE-VERIFIED end-to-end (2026-06-14)** against the running stack with the real `assistant`: `widget/start` grants 20 → guest question 201 → **assistant replies into the room** → balance depletes by words (clamped 0) → next send **402 `insufficient_tokens`**. **Bug found+fixed by live test:** economy config didn't fall back to `DEFAULT_CONFIG` (fresh install granted 0 → widget unusable); narrow `_economy_config()` fix + real-path tests, `--plugin meinchat --full` green (408u/67i); also fixed a latent double-hook-registration test leak. **Env notes:** enabled `bot_meinchat` (was off; gitignored plugins.json) + restarted api; assistant is provisioned **lazily** (provisioned manually for the test) — bot_meinchat populate should eagerly provision it (follow-on). **🟢 Eager assistant provisioning DONE** — `bot_meinchat` populate now idempotently provisions the `assistant` BOT user (DRY from `DEFAULT_CONFIG`; `on_enable` stays lazy); `--plugin bot_meinchat --full` + `--plugin meinchat --full` green. **🟢 UI WALKTHROUGH DONE** ([s86-WALK-REPORT-bot-widget.html](walkthrough/s86-WALK-REPORT-bot-widget.html), 6 browser shots, driver `walkthrough/s86-bot-widget-walkthrough.cjs`) — public guest: name prompt → Start → assistant replies in-room → token arc 200→167→133→99→…→0 → buy-block; 0 console errors. Setup findings: public pages are `CmsPost type=page`; layout area type `vue`; fe-user is Vite-HMR (no stale bundle). **Full report: [11](reports/11-s86-rooms-and-bot-widget.md).** **🟢 Widget = unified-import asset + docs (2026-06-14)** — shipped as `plugins/meinchat/docs/import/cms/widgets/meinchat-bot-widget.json` (envelope `cms_widgets`, slug `meinchat-bot-widget`); seeder rewired to import it via the `cms_widgets` exchanger (JSON = single source, upsert-by-slug, soft-dep), `--plugin meinchat --full` green (69i); imported live via `flask --app vbwd.app:create_app data-exchange import cms_widgets <file>` (DB-verified); docs `plugins/meinchat/docs/{merchant,developer,llm-agent}/bot-widget.md`. **Open (follow-on):** real LLM-bot consumer + multi-distinct-bot-users (seam ready: per-bot inbound-hook instances); S86.2b fe-crypto fan-out; public `/tokens` buy page; room rename UI + history-on-invite. Not committed. |
| **45.7** | [bot-discord adapter (native rich interactive content)](sprints/s45-7-bot-discord-rich-interactive-content.md) | NEW `bot_discord` (BE adapter) + `bot-discord-admin` (fe-admin) — deps `bot-base` | **PLANNED — 2026-06-14.** New adapter on the existing `IMessengerProvider` port (Open/Closed, no `bot-base`/consumer change): first **button-native** channel — renders the S70 `BotReply.meta` vocab (`bot_choices`/`bot_menu`/`bot_cart`) as Discord **buttons/select-menus/embeds** (plain `body` fallback, Liskov). Transport: prod **HTTP Interactions Endpoint + Ed25519** verify; dev TESTING-guarded gateway worker; idempotent slash-command sync. Two models — `DiscordBot` (encrypted token+public-key, D4) + `DiscordActionRef` (100-char `custom_id` workaround). fe-admin companion (secrets+entities). Re-proves cross-provider parity (chat/taro/subscription light up, zero edits). |
| **90** | [Close all exposures: production hardening, env/logging switch, RC-1 pre-rollout gate](sprints/s90-close-all-exposures.md) | `vbwd-backend` (route-exposure oracle, log-level switch, `prod-readiness` cmd) + `vbwd-fe-admin`/`vbwd-fe-user` (prod-noise) + `vbwd-demo-instances` (prod compose/nginx) + `docs/ops` | 🟠 **PLANNED — 2026-06-14 (release-gating for RC-1 / public beta).** Audit found the base is sound (consistent decorators, debug endpoints gated, prod secrets validated) → sprint = **verify + close gaps + durable guard**. Keystone **route-exposure oracle** (walks `url_map`; every route allow-listed-or-`@require_auth`, mutating routes need a permission marker) so a forgotten decorator can't ship public (G1). Real gaps: `LOG_LEVEL` only throttles gunicorn not the app logger → add `VBWD_LOG_LEVEL` min-level (G2, disk-fill fix); unbounded container access-log → Docker logging rotation (G3); duplicate unused `require_permission` (G4); webhook-sig + auth rate-limit (G5); `flask prod-readiness` cmd + RC-1 checklist + dev↔prod env-switch doc with screenshots (G6); fe prod-noise (G7). 4 owner decisions (log-level default, dead-code scope, access-log strategy, beta rate-limit). |
| **91** | [CMS on mobile: config-driven "Posts" browser for iOS (Android contract-ready)](sprints/s91-cms-ios-android-support.md) | `vbwd-ios` (new `vbwd-ios-plugin-cms` + 2 `vbwd_config.json` keys) + `vbwd-backend`/`vbwd-fe-user` `cms` (chrome-light embed render) + docs | **PLANNED — 2026-06-14.** Burger item **"Posts"** = a `WKWebView` (the app's first) over a host-rendered **chrome-light archive** of a `category × post-type` set by device config `root_ios_category_on_host` + `root_ios_post_type_on_host`. **One renderer (web) ⇒ every post type works** (page/post/video/pdf via `registerCmsPageType`), no per-type native code — the "by default a browser" reading. Adds only an `embed` render mode + a fail-loud `embed-manifest` in the **cms plugin** (no core change). JWT seeded into the WebView for access-gated posts. **Android app doesn't exist** → delivered as a platform-neutral contract + port checklist (Decision D-Android: defer the app). |
| **92** | [Referral base (umbrella): partner payment-provision tracking + internal VBWD referral coupons](sprints/s92-refferal-base.md) | **Track B (now):** NEW `referral` plugin (deps `discount`+`meinchat`) + additive `discount` event + `referral-admin` (fe-admin). **Track A (design):** NEW `vbwd-provision-base` service + provider-referral port | 🟠 **PLANNED — 2026-06-14. Two tracks.** **Track B (implementable):** meinchat bot cmd `/referral_program_new_coupon --coupon REF_USER_` (perm `meinchat_can:generate_coupons`) mints `<PREFIX>_<NICKNAME>_<HEX8BYTES>` (`secrets.token_hex(8).upper()`, e.g. `REF_USER_BOB_F45E2A5DBB6677FF`) cloning a selected discount template; **redemption** pays the issuer a token commission (% of sale or absolute) via core `TokenService`, idempotent, via a new additive `discount.coupon_redeemed` event; **Promotions → VBWD Referral** page (Settings: Issuer's-commission + `DualListSelector` template picker · Referral Statistics: generic table, codes **masked until used** `F45ExxxxxxxxxxFF`, search/sort/filter/bulk-delete/export CSV+JSON via `referral_coupons` exchanger). **Track A (B2B):** partner-provision ledger over Stripe Connect/PayPal Partner on a **separate container** — architecture + inert port only this sprint; live wiring deferred (needs platform accounts; DA-1…DA-4). Decisions: percent→tokens rate, self-referral reject, export-mask, refund claw-back risk. |

## Carry-forward / open

- **Bot epic packaging** — batch-create + push the 4 standalone `vbwd-plugin-*` repos (`bot-base`, `bot-telegram`, `bot-meinchat`, `fe-admin-bot-telegram`); still pending the owner's go-ahead (irreversible outward action).
- **45.5.1** — meinchat E2E bot-participant decrypt, blocked on meinchat-plus implementing `e2e_v1` Signal decrypt; S70 rich `meta` rides the **plain** path; S71 reserves the `{body,meta}` envelope shape.
- **Real Telegram smoke test (S45 Q2)** — ready whenever a @BotFather token is provided.
- **S67 real-device APNs proof** — acceptance §6 of [S67.2](../20260610/done/s67-ios-notifications-and-badges.md): physical device + Apple-portal Push capability on the provisioning profile + `.p8` key mounted on the backend (sandbox first). Code on both ends is gate-green; only the live wire is unproven.
- Optional **fe-admin styling UI** for `BotConversationStyle` (today: generic plugin config + Import/Export surface).

## Dev-stack note (reversible, 2026-06-11)

The storefront catalog is deactivated to the demo set (`is_active` toggle: Starter/Pro/Business · Priority Support/Extra Seats · 1k/5k/20k); `bot-meinchat` + `subscription.bot_storefront_enabled` on; a `Default` `BotConversationStyle` seeded; bot user `assistant` provisioned; S70 migrations applied to the dev DB. Plugins are gitignored/on-disk — nothing committed.
